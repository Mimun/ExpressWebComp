<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%- title %></p>
    <!-- <%-include(appConfig.footer)%> -->
    <%-include(footerView)%>
    <script>    
      var appConfig = <%- JSON.stringify(appConfig)%>;
      console.log(appConfig);
      console.log(<%- JSON.stringify(title)%>);      
    </script>
  </body>
</html>
